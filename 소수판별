#include <stdio.h>

int main() {
	int num, sum=0;
	
	scanf("%d",&num);
	
	for(int i=1; i <= num; i++){
		if (num%i == 0)sum++;
	}
	
	if (sum > 2 || num == 2)printf("False");
	else printf("True");
	
	return 0;
}
